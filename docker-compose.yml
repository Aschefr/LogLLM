services:
  logllm:
    build: .
    container_name: logllm
    ports:
      - "8081:8080"
    volumes:
      - ./appdata:/app/appdata
      - ./logs:/logs
    restart: unless-stopped
    networks:
      - logllm-network

networks:
  logllm-network:
    driver: bridge

volumes:
  ollama_data:
